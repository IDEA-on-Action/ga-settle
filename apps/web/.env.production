# 프로덕션 빌드 전용 (vite build 시 .env보다 우선). 커밋 대상.
# API는 apps/api Worker가 같은 오리진(ata.minu.best)에서 SPA와 함께 서빙 → 프로덕션 절대 URL.
# ⚠️ 이 값이 없으면 dev용 .env(VITE_API_BASE=http://localhost:8787)가 번들에 박혀 로그인 불가 (2026-07-09 장애).
VITE_API_BASE=https://ata.minu.best
