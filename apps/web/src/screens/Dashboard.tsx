import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function Dashboard() {
  return <PagePlaceholder title="대시보드" description="정산 사이클 현황(수집 → 매핑 → 계산 → 대사 → 마감) + 핵심 KPI" />;
}
