import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function Rules() {
  return (
    <PagePlaceholder
      title="시책 룰"
      description="룰 CRUD(POST/GET/DELETE /api/rules) · 지급액 diff 시뮬레이션(POST /api/rules/simulate)"
    />
  );
}
