{
  "name": "@ga-settle/schema",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": { "drizzle-orm": "^0.36.0", "zod": "^3.23.0" },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "drizzle-kit": "^0.28.0",
    "better-sqlite3": "^11.5.0",
    "@types/better-sqlite3": "^7.6.11"
  }
}
